apiVersion: v1
kind: Service
metadata:
  name: hello-world-rest
  labels:
    app: hello-world-rest
spec:
  type: NodePort
  selector:
    app: hello-world-rest
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
